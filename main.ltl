// ltl-statsd-client/main.ltl
//
// StatsD UDP emitter with counters, gauges, and timers.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-statsd-client v0.1.0")
    io::println("StatsD UDP emitter with counters, gauges, and timers.")
    0
}
